<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/root"
    tools:context=".Viewer"
    tools:ignore="MissingConstraints">

    <Toolbar
        android:id="@+id/toolbar"
        style="@style/toolbar"
        android:popupTheme="@style/Widget.InstaTools.Popup.Secondary"
        android:title="@string/vwTitle" />

    <FrameLayout
        android:id="@+id/frame"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/selectionCount"
        app:layout_constraintTop_toBottomOf="@id/toolbar" />

    <View
        android:id="@+id/tbShadow"
        style="@style/tbShadow" />

    <TextView
        android:id="@+id/selectionCount"
        style="@style/selectionCount"
        app:layout_constraintBottom_toBottomOf="parent" />

    <include
        android:id="@+id/expanded"
        layout="@layout/expandable" />
</androidx.constraintlayout.widget.ConstraintLayout>