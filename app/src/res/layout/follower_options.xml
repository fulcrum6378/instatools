<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/dialogViewGroup"
    android:paddingTop="35dp">

    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/alsoRequestPv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:checked="false"
        android:text="@string/alsoRequestPv"
        android:textSize="16sp" />

    <LinearLayout
        android:id="@+id/limitLl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="7dp">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/limitTv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="0.5"
            android:labelFor="@id/limit"
            android:text="@string/mfLimit"
            android:textColor="@color/defCA"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/limit"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="0.5"
            android:importantForAutofill="no"
            android:inputType="number"
            android:maxLength="4"
            android:maxLines="1"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:textAppearance="@style/TextAppearance.InstaTools.Bold"
            android:textColor="@color/defCA"
            android:textSize="16sp" />
    </LinearLayout>
</LinearLayout>