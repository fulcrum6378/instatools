<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ir.mahdiparastesh.instatools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/new_backup"
        android:fullBackupContent="@xml/old_backup"
        android:icon="@mipmap/launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InstaTools"
        tools:ignore="UnusedAttribute">

        <activity
            android:name=".Main"
            android:exported="true"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Login"
            android:exported="false"
            android:theme="@style/Theme.InstaTools.Secondary"
            android:windowSoftInputMode="stateHidden|adjustPan" />

        <activity
            android:name=".Downloads"
            android:exported="true"
            android:launchMode="@integer/dwLaunchMode"
            android:theme="@style/Theme.InstaTools.Secondary">
            <!-- except "singleInstance" all worked fine in Mum's tablet, until where TODO -->

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />

            <intent-filter android:label="@string/dwShare">
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Viewer"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.InstaTools.Secondary">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />

            <intent-filter android:label="@string/vwShare">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="www.instagram.com"
                    android:scheme="https" />

                <data
                    android:host="instagram.com"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Favourites"
            android:exported="false"
            android:theme="@style/Theme.InstaTools.Primary">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />
        </activity>

        <activity
            android:name=".MassFollower"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.InstaTools.Primary">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />
        </activity>

        <activity
            android:name=".Settings"
            android:exported="false"
            android:theme="@style/Theme.InstaTools.Tertiary">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Main" />
        </activity>

        <service
            android:name=".serv.Queuer"
            android:exported="false"
            android:stopWithTask="false" />

        <service
            android:name=".serv.Exporter"
            android:exported="false"
            android:stopWithTask="false" />

        <service
            android:name=".serv.Follower"
            android:exported="false"
            android:stopWithTask="false" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9457309151954418~5798988563" />
    </application>
</manifest>