plugins {
    id 'com.android.application'
    id 'kotlin-android'
    id 'kotlin-kapt'
}

android {
    compileSdk 32
    buildToolsVersion '33.0.0'

    defaultConfig {
        applicationId 'ir.mahdiparastesh.instatools'
        minSdk 21
        targetSdk 32
        versionName '20.8'
    }

    signingConfigs {
        aleph {
            storeFile file('D:\\Experimental\\mahdiparastesh.jks')
            keyAlias 'instatools'
            storePassword "${System.getenv("JERK")}"
            keyPassword "${System.getenv("JERK")}"
        }
        beth {
            storeFile file('D:\\Experimental\\mahdiparastesh.jks')
            keyAlias 'instatools_beth'
            storePassword "${System.getenv("JERK")}"
            keyPassword "${System.getenv("JERK")}"
            // In Google Play: alias(Deployment Key)=>instatools, signing-key-alias=>instatools_beth
        }
    }

    flavorDimensions('store')
    productFlavors {
        all { dimension 'store' }
        iran {
            versionCode 59 // 38
            signingConfig signingConfigs.aleph
            manifestPlaceholders = [useMainNormal: true, useTmCensored: false]
        }
        play {
            applicationIdSuffix '.beth'
            versionCode 29
            signingConfig signingConfigs.beth
            manifestPlaceholders = [useMainNormal: true, useTmCensored: false]
        }
        galaxy {
            applicationIdSuffix '.beth'
            versionCode 30
            signingConfig signingConfigs.aleph
            manifestPlaceholders = [useMainNormal: false, useTmCensored: true]
        }
    }
    sourceSets.iran {
        kotlin.srcDirs = ['src/main/kotlin', 'src/main/kotlin_iran']
        res.srcDirs = ['src/main/res', 'src/main/res_iran']
    }
    sourceSets.play {
        kotlin.srcDirs = ['src/main/kotlin', 'src/main/kotlin_play']
        res.srcDirs = ['src/main/res', 'src/main/res_play']
    }
    sourceSets.galaxy {
        kotlin.srcDirs = ['src/main/kotlin', 'src/main/kotlin_galaxy']
        res.srcDirs = ['src/main/res', 'src/main/res_play'] // Galaxy uses Play resources!!
    }

    buildTypes {
        debuggee {
            debuggable true
            minifyEnabled false
        }
        demo {
            debuggable true
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    android.variantFilter { if (it.buildType.name in ['debug', 'androidTest']) it.setIgnore(true) }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    kotlinOptions { jvmTarget = '1.8' }
    buildFeatures { viewBinding true }
}

dependencies {
    def room_version = '2.4.2'
    def glide_version = '4.13.2'
    def media2_version = '1.2.1'

    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'com.google.android.material:material:1.6.1'
    implementation 'com.android.volley:volley:1.2.1'
    implementation 'com.google.code.gson:gson:2.9.0'
    implementation "androidx.room:room-runtime:$room_version"
    kapt "androidx.room:room-compiler:$room_version"
    implementation "com.github.bumptech.glide:glide:$glide_version"
    kapt "com.github.bumptech.glide:compiler:$glide_version"
    implementation 'androidx.activity:activity-ktx:1.5.0' // Only for ActivityResultLauncher
    implementation 'org.apache.commons:commons-text:1.9' // StringEscapeUtils
    implementation 'com.airbnb.android:lottie:5.2.0'
    implementation 'com.facebook.shimmer:shimmer:0.5.0'
    implementation 'androidx.swiperefreshlayout:swiperefreshlayout:1.1.0'
    implementation 'androidx.recyclerview:recyclerview-selection:1.1.0'
    //implementation 'com.google.android.gms:play-services-ads:21.0.0'
    implementation "androidx.media2:media2-widget:$media2_version"
    implementation "androidx.media2:media2-player:$media2_version"
    //noinspection GradleDependency
    implementation 'com.tbuonomo:dotsindicator:4.2'
    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.6.3'
    implementation 'com.google.android.play:core:1.10.3'

    implementation 'com.github.fulcrum1378:ChipsLayoutManager:5140e08'
}
